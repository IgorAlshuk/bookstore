export enum routes {
  HOME = "/",
  DETAILS = "/books/:id",
  SEARCH = "/search/:title/:page",
  FAVORITES = "/favorites",
  CART = "/cart",
  ACCOUNT = "/account",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  NOT_FOUND = "*",
}
